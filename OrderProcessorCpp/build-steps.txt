# from repo root
rm -rf cmake-build-debug
cmake -S OrderProcessorCpp -B cmake-build-debug
cmake --build cmake-build-debug --config Debug

cd cmake-build-debug

# List all tests registered in CTest:
ctest -N

# Run all tests verbosely (you should see each gtest case run separately)
ctest --output-on-failure -V